# AI指挥家 — 功能需求任务队列

> 此文件记录用户提出的所有功能需求，AI指挥家在执行前必须先将需求同步到此文件并提交至仓库。

---

## 任务队列

| 编号 | 需求描述 | 优先级 | 状态 | 提出时间 | 完成时间 |
|:---:|:---|:---:|:---:|:---:|:---:|
| F001 | 参数模拟器求解数据应按颜色优先级排产 | HIGH | 已完成 | 2026-03-05 | 2026-03-05 |
| F002 | 优化求解算法，严格禁止任何约束超限 | HIGH | 已完成 | 2026-03-05 | 2026-03-05 |
| **F003** | **将当前的求解算法方案以md格式单独写出到仓库中** | MEDIUM | **已完成** | 2026-03-05 | 2026-03-05 |
| **F004** | **参数模拟器严格按照rule.xls中的连续排产的每一期规则计算** | HIGH | **已完成** | 2026-03-05 | 2026-03-05 |
| **F005** | **优化参数模拟器中二加的可用机器和可用人数的计算显示** | MEDIUM | **已完成** | 2026-03-05 | 2026-03-05 |
| **F006** | **生成一份rule.xls连续排产的每一期中每一个数据的表格计算公式** | MEDIUM | **已完成** | 2026-03-05 | 2026-03-05 |
| **F007** | **将产品参数ABCD的机器时人力时原材料改为可输入变量** | HIGH | **已完成** | 2026-03-05 | 2026-03-05 |
| **F008** | **将产品参数面板融入参数模拟器内部，配合方案变化实时联动** | HIGH | **已完成** | 2026-03-05 | 2026-03-05 |

---

## F003: 求解算法文档化

- **需求描述**: 将 `engine.ts` 中的求解算法 `optimizeShiftPlan` 完整逻辑以 Markdown 格式写出，便于理解和交接。
- **验收标准**: 创建 `SOLVER_ALGORITHM.md`，包含算法概述、核心原则、多比例搜索策略、约束检查点等内容。
- **完成状态**: 已完成，文档已提交至仓库。

---

## F004: 参数模拟器规则验证

- **需求描述**: 严格验证参数模拟器的计算逻辑（约束、颜色映射等）与 `rule.xls` 完全一致。
- **验收标准**: 逐项对比 Excel 公式与 `engine.ts` 代码，确保无偏差。
- **完成状态**: 已完成，验证结果显示代码与 Excel 规则完全一致，无需修改。

---

## F005: 优化二加资源显示

- **需求描述**: 在参数模拟器中，优化二加班次的可用机器和可用人数显示，使其更直观。
- **验收标准**: 在二加行的表格和约束卡片中，增加"可用上限"和"已用"信息。
- **完成状态**: 已完成，`PeriodResult` 接口已扩展，`SimulatorTab.tsx` 已更新显示逻辑。

---

## F006: 生成 rule.xls 公式文档

- **需求描述**: 生成一份 `rule.xls` 连续排产每一期每一个数据的表格计算公式，以 Markdown 格式呈现，要求直观易懂。
- **验收标准**: 创建 `RULE_FORMULAS.md`，包含颜色定义、产品规格、右侧参数区公式、产量颜色映射矩阵、6个约束检查点公式等。
- **完成状态**: 已完成，文档已提交至仓库。

---

## F007: 产品参数可输入变量

- **需求描述**: 将产品 A/B/C/D 的机器时、人力时、原材料从固定常量改为可输入变量，用户可在参数模拟器中自由修改。
- **验收标准**:
  1. `data.ts` 中新增 `ProductSpecInput` 接口和 `DEFAULT_PRODUCTS` 常量
  2. `engine.ts` 中新增 `setActiveProducts()` 函数，所有计算引擎使用动态产品参数
  3. `SimulatorTab.tsx` 中新增可折叠的「产品规格参数」面板，包含 4×3 输入表格
  4. 修改值后自动高亮显示，右侧自动计算「机器/单位」和「人力/单位」
  5. 支持「恢复默认值」按钮
  6. 重置参数时同步重置产品参数
- **完成状态**: 已完成，构建通过，UI 验证通过。

---

## F008: 产品参数融入模拟器联动

- **需求描述**: 将产品参数 ABCD 可输入变量面板从独立区域移入参数模拟器的全局参数面板内部，确保修改产品参数后所有排产计算（约束验证、产量计算、最优求解）实时联动变化。
- **验收标准**:
  1. 产品规格参数面板嵌入全局参数面板内部（紫色虚线边框折叠按钮）
  2. `defaultResult` 依赖 `productSpecs`，对比模式下默认方案随产品参数变化更新
  3. 修改产品参数后，`calculateProduction`、`optimizeAllPeriods`、`optimizeShiftPlan` 全部使用新参数
  4. 使用 `AnimatePresence` 实现展开/折叠动画
  5. 底部提示文字说明联动关系
- **完成状态**: 已完成，构建通过，UI 验证通过。

---

## 历史任务记录 (F001, F002)

...(此处省略 F001 和 F002 的详细描述和日志，以保持文件简洁)...
